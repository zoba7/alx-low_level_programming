#include <stdio.h>

/**
 * main - print program name
 * @argc: number of param
 * @argv: array
 * Return: return 0
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
